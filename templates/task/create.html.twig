{% extends 'base.html.twig' %}

{% block title %}Ajouter une tache{% endblock %}

{% block body %}
    <div>
        <h1>Ajouter une nouvelle tache</h1>
        {{ form_start(taskCreateForm) }}
        {{ form_widget(taskCreateForm) }}
        <button type="submit">Ajouter</button>
        {{ form_end(taskCreateForm) }}
    </div>
{% endblock %}